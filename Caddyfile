{
    email radionovich.arkadiy@mail.ru
}

https://xn----dtbhcghdehg5ad2aogq.xn--p1ai, https://www.xn----dtbhcghdehg5ad2aogq.xn--p1ai {
    encode gzip

    reverse_proxy app:3000 {
        # Увеличенные таймауты для медиа-файлов
        transport http {
            response_header_timeout 120s
            read_timeout 120s
            write_timeout 120s
            dial_timeout 30s
        }

        # Заголовки
        header_up Host {host}
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-For {remote_host}
        header_up X-Forwarded-Proto {scheme}

        # Health check (опционально)
        health_uri /api/health
        health_interval 30s
        health_timeout 10s
    }

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Frame-Options "SAMEORIGIN"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    # Логирование для отладки (можно убрать после исправления)
    log {
        output file /var/log/caddy/access.log {
            roll_size 100mb
            roll_keep 5
        }
        format json
        level INFO
    }
}
